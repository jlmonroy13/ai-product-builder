# Stage 7 — Planning

Follow **exactly** `ai-builder/system/prompts/7-planning.md` (role, rules, output format).

- Resolve `{{ARTIFACTS_PATH}}` as `projects/default-project/artifacts` (see `pipeline.config.json`).
- Required inputs are listed in that prompt.
- Save the stage output to `projects/default-project/artifacts/7-PLANNING.md` when persisting artifacts.
