# Stage 9 — Execution feedback

Follow **exactly** `system/prompts/9-feedback.md` (role, rules, output format).

- Resolve `{{ARTIFACTS_PATH}}` as `projects/default-project/artifacts` (see `pipeline.config.json`).
- Required inputs are listed in that prompt.
- Save the stage output to `projects/default-project/artifacts/9-EXECUTION-FEEDBACK.md` when persisting artifacts.
