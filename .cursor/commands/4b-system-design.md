# Stage 4b — System design

Follow **exactly** `system/prompts/4b-system-design.md` (role, rules, output format).

- Resolve `{{ARTIFACTS_PATH}}` as `projects/default-project/artifacts` (see `pipeline.config.json`).
- Required inputs are listed in that prompt (discovery, tech preferences, optional UI validation).
- Save the stage output to `projects/default-project/artifacts/4-SYSTEM-DESIGN.md` when persisting artifacts.
